// Schema modules are added per-feature in subsequent commits:
//   T5  — auth (users, sessions, accounts, magic_link_tokens)
//   T8  — household + members
//   T11 — symptom_entries + assessments + triage_results
//   T13 — ai_calls + audit_logs
export {};
