# Server-side Gemini API key (used by /api/chat and other server routes).
# Never prefix this with NEXT_PUBLIC_ — it must not ship to the browser.
GEMINI_API_KEY=

# Legacy: still required by analyzeSymptoms() and generateFollowUpQuestions()
# in src/lib/gemini.ts until those are migrated server-side (Task T3).
NEXT_PUBLIC_GEMINI_API_KEY=
